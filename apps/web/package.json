{
  "name": "@open-video-trans/web",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "lint": "echo 'no linter configured yet' && exit 0",
    "test": "vitest run",
    "typecheck": "svelte-check --tsconfig ./tsconfig.json",
    "build": "vite build"
  },
  "devDependencies": {
    "@sveltejs/vite-plugin-svelte": "^4.0.0",
    "jsdom": "^25.0.1",
    "svelte": "^5.1.0",
    "svelte-check": "^4.0.0",
    "typescript": "^5.5.0",
    "vite": "^5.4.0",
    "vitest": "^2.1.8"
  }
}
