[project]
name = "local-runner"
version = "0.0.1"
description = "Local CLI runner stub for OpenVideoTrans (Tier 1 MVP skeleton)"
requires-python = ">=3.11"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/local_runner"]
