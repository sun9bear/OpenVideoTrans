# IMPLEMENTATION_LOG — OpenVideoTrans Tier 1

> 实时进度真源（workflow §11）。每批次一行：日期 / 单元 / 分支 / PR / 外审 / 结论 / 下一步。
> 状态另见：GitHub issue `status:*` 标签 + EPIC #30 checklist。重启/压缩后按 workflow §11 resumption 协议据此 + EPIC + 标签重建状态再续跑。
> **授权范围：** i18n 闸由项目主 2026-06-23 开启，授权自主推进至 M1（已达成、待验收）。**M2 由项目主 2026-06-25「开 M2」授权**；M2→后续里程碑边界仍待 sign-off。

## M1 — 本地管线打通

| 日期 | 单元 | 分支 | PR | 外审 | 结论 | 下一步 |
|---|---|---|---|---|---|---|
| 2026-06-23 | STEP0-A（monorepo 工具链骨架 + 3-job CI；含 i18n 闸开启记录） | feat/step0-a-monorepo-scaffold | #31（squash `214bd09`） | CodeX CLI（P2 pydantic→stdlib dataclass，已修）+ @CodeX bot（P1 i18n 闸记录，已解+resolve）+ Claude 4 维自审（.gitattributes 钉 LF） | ✅ 合并；CI 全绿（本地 + GitHub Actions） | STEP0-B |
| 2026-06-23 | STEP0-B（`contracts.schema.json` 真源 → Pydantic v2 + TS 确定性 codegen + codegen-diff 门；`packages/schemas` 接成 TS+Py 双包 `ovt_schemas`） | feat/step0-b-schemas | #32（squash `8f2c613`） | CodeX CLI（3×P2：required+default 转可选 / `extra=forbid` / 包入口；+ strict P2）+ @CodeX bot（复审 clean）+ Claude 4 维自审（确认 schema 忠实方案§3） | ✅ 合并；CI 全绿（本地 + GHA） | STEP0-C |
| 2026-06-23 | STEP0-C（红线护栏 CI 先接：5 付费安全不变量 xfail + autodub-core 边界 lint + redline job + marker） | feat/step0-c-redline-ci | #33（squash `29e64b6`） | Claude 4 维自审（抓 1 **blocking**：边界 lint 漏 `from . import gateway` 相对/动态 import → 已修 + 回归夹具）+ CodeX CLI（P1 xfail `raises=ImportError` 太宽 → 改 `xfail(not _API_READY, strict)`；P2 漏 bare `import_module('x')` → 加 `ast.Name` 检测）+ @CodeX bot（复审 clean） | ✅ 合并；CI 全绿（本地 + GHA；65 过 + 5 xfail，redline 20 过 + 5 xfail） | T1.1 ∥ T1.2 |
| 2026-06-24 | T1.1（autodub-core 移植：`config`/`ffmpeg_utils`/`stages` 拷改进 `packages/autodub-core`；复用 `ovt_schemas` 契约；provider seam 依赖注入；`ingest` 仅本地文件、URL/yt-dlp 推迟 T1.4） | feat/t1.1-autodub-core | #34（squash `1adb87b`） | Claude 4 维自审（0 blocking，2 候选对抗驳回，5 minor 吸收）+ CodeX CLI **11 轮**（**P1 红线**：`run_pipeline` 去 `allow_paid` 调用方入口、内核恒传 False；+ ~16 P2 resumability/原子性：tts/mux 缓存门、ingest 同文件/旧后缀/陈旧音频、TTS raw 原子、`atomic_output` 统一所有媒体写、align/tts 元数据持久化早于 skip-gate、demucs ambient 先写；+ 1 P3 demucs 临时目录泄漏）→ 收敛 | ✅ 合并（#34 squash `1adb87b`）；本地全绿（88 过 + 5 xfail；含 e2e smoke 真出 mp4+srt；golden 守 `assign_timing`/`atempo_chain_for`/`stitch_timeline` 帧级/SRT）；ruff/pyright/codegen-diff 净 | T1.2 ∥ T1.3a–g |
| 2026-06-24 | T1.2（provider-adapters：完整 ASR/MT/TTS 适配器 + ladder + `select()` 三重 guard + 完整 `PAID_PROVIDERS`；STEP0-C 5 红线不变量翻必绿、xfail TODO 清零；ASR 云优先 `groq→cloudflare→faster_whisper`、TTS `piper` 默认；与 core `Resolver` seam 结构兼容、两侧零 import edge） | feat/t1.2-provider-adapters | #35（squash `9c42c4c`） | Claude 4 维自审 + CodeX CLI **5 轮**（**P1 红线**：DeepL Pro key 不自动计费——`available()` 仅认 `:fx` free key + `translate()` 硬编 `api-free`；+ 多 P2）+ @CodeX bot（**P1 红线**：付费 MT 批失败 fail-to-error 不静默逐行重试——CLI 5 轮漏抓、bot 抓的）→ 收敛；**deferral→T1.3f**（DeepL 目标码 + 按目标语 provider gating）、**chunker→T1.3e** | ✅ 合并（#35 squash `9c42c4c`）；本地全绿（116 过 + **0 xfail**；redline 25 过）；ruff/pyright(0/0/0)/codegen-diff 净 | T1.3a–g（∥，各依赖 T1.1·T1.2）|
| 2026-06-24 | **T1.3 Batch A**（autodub-core 硬化 4 子单元，单分支顺序原子 commit）：T1.3a 隔离与付费钉死（`isolation.py`：job 命名空间 + 路径包含校验 + `pin_resolver` allow_paid 钉死；`manifest.py` 写 manifest）· T1.3c ffmpeg SSRF（`-protocol_whitelist file,crypto` 全命令 + 格式 allowlist）· T1.3d 输出模式条件管线（subtitle_only 跳 tts/align + 双语 SRT + 烧字幕 flag 占位）· T1.3b AIGC 标识 mux（按 output_mode：配音 AV 语音标 / 字幕机翻披露 + 记 manifest）；+ ffmpeg 防御加固（stitch 规范格式断言 + probe 畸形输出包 FfmpegError，对抗审发现、项目主直接并入 `ce7a756`） | feat/t1.3-kernel-hardening（11 commit：a/c/d/b + 对抗审`7b44568` + R1`3c6ee57` + ffmpeg`ce7a756` + R2`8022ae2` + R3`2bfdd77` + R4`4b09c8a` + R5`2b81f84`） | #36（squash `d093ec9`） | 我亲做（a/c 红线）+ **对抗多透镜审 workflow**（6 confirmed 全修）+ **CodeX CLI R1–R5 共 12 真问题全修**（severity R4 P1→R5 P2，已收敛）：R1 job 权威化/缓存 applied · R2 SSRF 前拒 playlist/Win 末点/mp4 metadata · R3 **§3 默认开**/concat -format_whitelist/标识缓存感知 + _run UTF-8 · R4 SSRF allowlist 补全所有源解析边界(ingest 缓存 + mux) · R5 mux 缓存键含字幕设置/命名空间拒 Win 非法名 → @CodeX bot R6+R7（红线 P1 复审已解 + 披露 cue 重叠 P2 + 双语保两行 P3 全修，bot 终审 clean）→ ✅ 收敛 | 本地全绿（autodub-core 201 过；全仓 **248 过 + 0 xfail**；redline 25 过；ruff/pyright(0/0/0)） | ✅ 合并 #36（squash `d093ec9`）→ Batch B |
| 2026-06-24 | **T1.3 Batch B**（provider-adapters 硬化 e/f/g）：T1.3e 云 ASR compress-first + 超限切块 offset 合并 + provider 格式协商（单次编码）· T1.3f `language_capabilities` registry（按 output_mode 分层 + BCP-47 + DeepL 逐语 vet + commercial-safe dub gate + 源语回填）· T1.3g 供应链 sha256 pin + 非商用模型禁入默认镜像 | feat/t1.3-provider-hardening（9 commit：e/f/g + R2–R9） | #37（squash `7967e7a`） | 对抗多透镜审 workflow（2 P2）+ CodeX CLI/​@CodeX bot **9 轮**（红线 P1 付费 ASR 不自动批量切块 + 8 P2 全修：双 encode/变体折叠/segment-only/OpenAI 编码/edge_tts 许可/text-only×2/ffmpeg avail/en-GB 地区码）→ 收敛；**剩 4 项运行期接线归 T1.4**（语言 gate 接准入 · 供应链 pin 接 doctor · commercial-safe 运行期 TTS 选择 · CF 源语解析）——按内核↔provider-adapters 硬边界，当前优雅降级 | ✅ 合并 #37（squash `7967e7a`）；本地全绿（全仓 296 过；redline 25 过；ruff/pyright(0/0/0)/codegen 净）| T1.4（= M1）|
| 2026-06-24 | **T1.4 = M1**（`cli/local-runner` 端到端）：薄 CLI `ovt run/doctor`——准入（`assert_language_pair` fail-closed + commercial-safe TTS 运行期选择，接 Batch B 4 项 deferral 的前 3）+ doctor preflight（`verify_piper_model` + 许可 gate，接第 4 供应链）+ URL/yt-dlp ingest（SSRF：非 http(s) 拒 + --no-playlist + T1.3c 格式 allowlist，仅此开）+ `run_pipeline`+`Resolver` → 带标识 mp4+srt；marking 默认开 | feat/t1.4-local-runner（off `7967e7a`，R1–R3） | #38（squash `8891ea2`） | 对抗多透镜 workflow（2 P2）+ CodeX CLI/​@CodeX bot **3 轮**（R1 5 项 / R2 3 CLI + 2 bot / R3 **CI pyright 红修**(漏测 tests/) + doctor 强制 ffmpeg pin；2 P1 红线-邻近 + 多 P2 全修）→ **bot R3 clean + CI 全绿**；2 更深限制按 scope 归延后单元（显式 --out 缓存键控=T1.1/M2 · piper per-language voice catalog=T1.3b/f） | CI 全绿（TS/Py/redline/codegen）；本地全仓 **322 过** 含 e2e 真出带标识 mp4+srt；redline 25 过；ruff/pyright(0/0/0，含 tests)/codegen 净 | ✅ 合并 #38（squash `8891ea2`）= **🎯 M1 达成、待项目主验收** |

## M2 — 云闭环（控制面 + worker 认领）

> 授权：项目主 **2026-06-25「开 M2」**。T2.0 = M2 硬门槛，先于 T2.1 建任何控制面（backlog DAG）。

| 日期 | 单元 | 分支 | PR | 外审 | 结论 | 下一步 |
|---|---|---|---|---|---|---|
| 2026-06-25 | **T2.0（M2 硬门槛 · 强校验，§3 我亲做）**（D1-claim 并发 spike：乐观锁 job claim 在并发下 exactly-once；本地 SQLite + 真实 Cloudflare D1 remote **两半都过才算硬门**）：可移植 `CLAIM_SQL`（claimable guard 外层 WHERE 重复 → 写串行下第二抢者 0 行）；本地 20×100 race + 重复 8 轮 + 过期可重领 + attempt 撞顶；远端同 SQL（仅 retarget 表名到隔离表 `_ovt_spike_jobs`，不碰 T2.1 的 `jobs`）经 D1 REST 跑 race + reclaim/cap 双场景；token 仅 Authorization header（GH log `***` 脱敏）、纯 stdlib urllib、429/5xx/URLError 退避、param 串化合 D1 REST 契约；workflow 仅手动 `workflow_dispatch` + concurrency 串行 | feat/t2.0-d1-claim-spike（本地半 `ef990a0` + 远端半 `0b2378e` + 审修 `62f9038`/`fc021ff`/`3c2d751`/`175c6c9`） | #40（squash `ec09b6e`） | 对抗多透镜审 workflow（4 镜 19 raw → 验真 3：**P1 远端补齐三不变量**〔reclaim+cap 真在 D1 跑〕+ P2 HttpD1Client 离线全覆盖 + P2 瞬态重试）+ CodeX CLI（2 P2：本地线程异常不再被吞 + statement 级 `success:false` 不再读成空）+ @CodeX bot **3 轮**（R1 P2 param 串化 + P1 pyright extraPaths〔CI 实测已绿属误报、仍按建议显式加固〕；R2 P3 远端 spike 串行化）→ 收敛 | 本地全绿（全仓 **342 过**；ruff/pyright(0)；spike 纳入 CI pyright）+ **真实 D1 remote PASS**（race 100/100 no-double=True · reclaim+cap [1,2,3] ok=True · RESULT: PASS，run 28142927440） | ✅ 合并 #40（squash `ec09b6e`）；**M2 硬门槛两半皆绿** → 解锁 T2.1 |
| 2026-06-25 | **T2.1（control-plane Worker · 强校验 §3 我亲做）**：CF Worker 整片端点（`/api/uploads/sign` SigV4 预签 PUT〔零依赖 Web Crypto〕+ PUT 后 HEAD 校验 size/type〔不符删对象不建 job〕+ `/api/jobs` CRUD〔v4 全字段〕+ `/internal/jobs/claim`〔§8 确定性 comparator：字幕>配音→aging 桶 floor→advisory 升序→enqueue→job_id；复用 T2.0 乐观锁〕+ `progress` 心跳〔claim_version 门控〕+ `complete`/`fail` 幂等〔首终态胜、产物 key 绑 claim_version〕+ `/api/jobs/:id/download/:artifact`〔SigV4 预签 GET，归属+完成+过期+清盘校验，封顶剩余 TTL〕+ `/internal/config` + `/internal/credentials`〔inert 501〕）+ 完整 D1 schema(jobs/upload_sessions) + queue_adapter=D1-claim；`/internal` 共享密钥 fail-closed 恒时比较 | feat/t2.1-control-plane（impl `f58ac56` + 审修 `c6b10dd`/`21aac34`/`9d421fd`/`dc4c0e4`） | #41（squash `e12bfcb`） | 对抗多透镜 workflow（6 镜→验真 1：aging 浮点/floor 偏差）+ CodeX CLI（4：**P1 上传消费原子化** + aging 整数 floor〔恢复同桶 advisory SPT tiebreak〕 + P2 产物 key 绑 claim_version + P2 隐藏 error_detail）+ @CodeX bot **R1**（3，文档核实在 scope：**P1 /api 路由契约对齐** + P2 HEAD type 校验 + P2 下载封顶 TTL）+ **R2**（2 P2：HEAD type fail-closed〔无 content-type 也拒〕 + 拒清盘下载）+ **R3**（1 **P1 上传 swap TOCTOU** → 上传 PUT TTL 收窄 15min + 路由 T2.4）→ descending 收敛（项目主拍板「合并+路由 T2.4」） | 本地全绿（control-plane **40 测**〔vitest+better-sqlite3，真 CLAIM_SQL 跑真 SQLite 引擎〕 + typecheck/lint/build；全仓 Py 342 + redline 25 + schema no-drift 不受影响）+ **CI 4 job 绿** | ✅ 合并 #41（squash `e12bfcb`）；解锁 T2.2/T2.3/T2.4/T2.6 等（依赖 T2.1）。**归 T2.4：上传对象不可变 / worker ffprobe 复核**（HEAD 后 swap 由 worker 认领时 ffprobe 准入端到端闭合，冻结设计 §pipeline 行 52；控制面 HEAD = 快速准入过滤 + verified_bytes 快照） |
| 2026-06-25 | **T2.2（桩 worker · Python）**：media-worker 从骨架落地为可运行桩（plan §6 契约）：`claim → 取源(R2) → 输入原样拷成输出(不跑真管线，留 M2-CLOSE) → complete`；幂等 complete/fail 走 T2.1 `claim_version` 门；产物 key=`artifacts/{job}/{cv}/`；**30s 独立心跳线程续租**（`threading.Event` 计时器，不靠 stage 边界；硬封顶 `job_hard_timeout_sec` 停续租 + 精确 elapsed 判→`processing_timeout`、不误标 done）；完成上报瞬时错误不误判失败（留租约恢复）；**try/finally 必清 workdir + 启动清孤儿**；workdir 按 `<job>__<cv>` cv 隔离（同机重领不互删）；命名空间/路径包含复用 autodub-core `safe_component`/`ensure_within`；**零三方依赖**（stdlib urllib + hmac/hashlib SigV4 签 R2〔AWS KAT 钉死〕+ ovt-schemas 解 Job）；R2 凭据/internal token 仅签名/Authorization、绝不入 URL/日志/错误体；source_key 由 `upload_session_id` 复推（schemas Job 无 source_key，对齐 uploads.ts，含漂移守测） | feat/t2.2-stub-worker（impl `4eb3442` + 审修 `8322822`/`719c82b`/`54bb5d9`/`e0cdfbc`/`0188803`） | #42（squash `46c2201`） | 对抗多透镜 workflow（3 镜 security/concurrency/contract → 0 P1 + 小硬化：签 Content-Type + 校 upload_session_id + 日志卫生）+ CodeX CLI **5 轮**（R1 both 双产物·完成错误不误判；R2 心跳硬封顶·启动配置兜底；R3 超时不误判完成·配置按 claim 刷新；R4 硬超时精确门；R5 workdir cv 隔离·多进程共卷清孤儿=文档化越界）→ descending 收敛、超时主题闭 → @codex bot 终审 **clean**（"no major issues"，`0188803`） | 本地全绿（media-worker **32 测** pytest〔含 SigV4 KAT〕；全仓 Py 373 + redline 25 + schema no-drift；ruff/pyright(0)）+ **CI 4 job 绿** | ✅ 合并 #42（squash `46c2201`）；解锁 OBS(#24)·DEVLOOP(#25)（依赖 T2.1·T2.2）。**归 DEPLOY/M2-CLOSE：多 worker 进程共享 OVT_WORKDIR 的清孤儿 ownership/lock 硬化**（部署=单容器/箱独占卷、全清正确；多进程共卷属 ops 范畴） |
| 2026-06-25 | **T2.3（sweeper · CF Cron）**：控制面加 `scheduled` 入口 + `triggers.crons`（每分钟），四职责保 worklist+存储一致（plan §6/§177/§261-264/§92）：①**TTL 清（AD-17）**终态 job（done/failed）过 `expires_at` → 按 `artifacts/<job>/` **前缀** list+delete 全 attempt 产物（反 reclaim 留下的陈旧 cv 产物，不在 jobs.artifacts）+ 源、盖 `data_purged_at`、status 不变（无 expired 态）·②**租约过期重排（H1）** running+租约过期：`attempt<max`→`queued`/否则 `worker_lost` 终态（**唯一**终结 attempt 耗尽丢失 worker 的路径，claim 的 `attempt<max` 会让其永卡 running）·③**上传孤儿清** pending 会话过 1h TTL → claim(`pending→expired` 串行守 vs verifyUpload consume)→删源→盖 `source_purged_at`·④**queue reconciler** D1 为权威 worklist、stale `queued` 仍可 long-poll claim 领起（CF Queues 重唤醒路由 T2.5）；**claim_version 守**：重排后 worker 的 complete/fail/心跳均 409 拒（修 jobs.ts complete/fail 漏 meta.changes 检查的假成功 + cv1 产物泄漏）；各职责隔离 + 逐行隔离 + 批量界（IN(SELECT…LIMIT)），清理抖动/单行失败不饿死 H1/不钉 backlog | feat/t2.3-sweeper（impl `6948c77` + 审修 `59932ba`/`9fe14ce`/`2077cfb`/`bb1be92`/`ddf308d`） | #43（squash `342b923`） | 对抗多透镜 workflow（5 镜→验真 3：complete/fail 409 守 cv + runSweep 职责隔离 + deadline_at 注释指 M2-CLOSE）+ CodeX CLI **5 轮 descending**：R1 生命周期/竞态×2（purge 限终态〔非终态留源〕+ 孤儿 claim-then-delete）→ R2 删失败可重试 → R3×3（崩溃幂等 expiring 标记 + 陈旧 cv 前缀清 + 租约恢复批量界）→ R4 重试标记移出契约枚举（'expiring'→内部列 `source_purged_at`）→ R5 前向迁移 0002〔不改已上 main 的 0001〕+ purge 逐行隔离 → §7 停 CLI 环 → @codex bot 终审 **clean**（"no major issues"，`ddf308d`） | 本地全绿（control-plane **64 测** vitest〔sweep 24，含杀-worker H1/批量界/409 守 cv/前缀反陈旧cv/崩溃留 unpurged 重试/竞态守〕 + tsc 0；全仓 Py 373 + redline 25 + schema no-drift 不受影响）+ **CI 4 job 绿** | ✅ 合并 #43（squash `342b923`）；解锁 **T2.5**（CF Queues fast-follow，依赖 T2.1·T2.3）。**归 M2-CLOSE：`deadline_at` 跨模式档反饿死**（需 comparator/错误码，§12 调度 DoD）；**归 T2.4/M2-CLOSE：`worker_lost` 配额退还**（需双池 counters 表） |
| 2026-06-26 | **T2.6（前端最小 UI · Svelte 5 + Vite · M）**：匿名优先 SPA（仅 `apps/web/**`）选文件→`/api/uploads/sign`→直传 R2→`/api/jobs`→轮询 `getJob`→**懒签下载**。匿名优先：随机 `anon_id` 首方 `SameSite=Strict` cookie + `X-OVT-Anon-Id`（`crypto.randomUUID` 在 HTTP 非 localhost 回退 `getRandomValues`；畸形 cookie try/catch 容错）；**输出模式选择器**（字幕/配音/双语）；字幕 burned **disabled「即将支持」**(M2.1)；**长视频警示**（镜像 server cap、ffprobe 权威）；**AIGC 披露默认开前端不可关**(红线3) + 排队/限额/保留期/隐私文案；UI 先中文。**Turnstile**(配 `VITE_TURNSTILE_SITE_KEY` 渲染、**上传后取 token** 避免慢传过期、加载失败/配置失配/被拒全 fail-closed 无重传)；**MIME 扩展名优先**映射规范类型（修空/别名 415）；client 镜像仅建议不硬门；下载点击懒签（恒在 presign TTL 内） | feat/t2.6-web-ui（impl `ee4e2f7` + 对抗审修 `d1769bc` + CLI R1-R5 + bot `0872562`） | #46（squash `557e734`） | 对抗 workflow（5 镜→**3 confirmed**：轮询竞态+MIME×2 修）+ **CodeX CLI 5 轮 descending**（R1 P1 Turnstile token 缺失+P2 crypto→R2 3×P2〔token 过期/API base 尾斜杠/镜像硬门〕→R3 2×P2〔widget 加载失败/轮询 gen〕→R4 2×P2〔park-on-fail/无重传〕→R5 2×P2〔MIME 别名/配置失配〕；**§7 CLI 达 5 轮 cap 停**）+ **@codex bot 终审** R1 3 项（下载懒签+cookie 容错已修、CORS 跨单元路由）→R2 仅余 CORS 文档化延后=收敛 | 本地全绿（**vitest 29**〔caps/session/modes/mime/api/turnstile + App mount smoke〕+ svelte-check 0 + vite build；frozen-lockfile 通过；control-plane/Py 不受影响）+ **CI 5 job 绿** | ✅ 合并 #46（squash `557e734`）。**归 SECRETS：anon_id 服务端 HMAC 签名/验证**（需 server secret）；**归 DEPLOY：Turnstile token 生命周期 live-gate UX 验证**；**归 control-plane/DEPLOY：跨域 CORS**（前端无法加 ACAO、默认同源）。下一就绪：SECRETS(#21)·CFG-GUARD(#22)·FREE-POOL(#23)·OBS(#24)·DEVLOOP(#25) |
| 2026-06-26 | **T2.5（queue_adapter 换 CF Queues Free + 瘦 consumer · S）**：D1-claim → CF Queues Free + 瘦 consumer，证桥接（**D1 仍权威 worklist、Queues 仅唤醒/降延**）。**生产者 seam**：`D1ClaimProducer`(no-op) / `CfQueuesProducer`(best-effort send，吞 send 错绝不让 createJob 失败)；`selectProducer` 在 cf_queues 选中但绑定缺失时 **fail-safe 回落 D1**（不抛不孤立）。**瘦 consumer**（`queue()`→`handleQueueBatch`）对 wake 按 D1 权威态**只读 reconcile、从不 claim/改 job 态**（worker 乐观锁是唯一认领点→无双处理；at-least-once 重复无害）：live→ack / stale·terminal→ack-drop / 瞬时 D1 错→retry。createJob 权威 INSERT 后发 best-effort wake；`queueBackend` 默认 `d1`（CFG-GUARD 锁生产）；**sweeper 只对 requeue 真状态转移**（`recoverLeases` RETURNING job_id）重发 wake（消除 stale 风暴），stale-queued 交 D1 长轮询兜；wrangler queues 生产/消费绑定。**不变量**：claimNext 恒走 D1 CLAIM_SQL、不读队列 → wake 过期/丢失**不孤立 job** | feat/t2.5-cf-queues（impl `ad01527` + 审修 `ebaea78`/`2f66ea9`） | #45（squash `a90a3d4`） | 对抗多透镜 workflow（5 镜→验真 **0 confirmed**，均 by-design 误报）+ CodeX CLI **R1 2×P2**（worker 不消费 wake / sweeper 不重发）→**R2 clean**：P2-2 修（sweeper 重发）；**P2-1=跨单元限制**（worker 订阅队列属 worker 改动、越 T2.5 文件 scope）路由 DEPLOY/M2-CLOSE + queue.ts 文档化 · @codex bot **R1 P2**（cf_queues 下 reconcile 每 tick 重发 stale = 风暴）→改为只重发 requeue 真转移；**R2 互斥 P2**（又要求重发 stale）= **§7 circuit-breaker**（两轮互斥、coherent 同满足需 `last_wake_at` 节流标记 + 仅 worker 订阅后才有功能效果）→经评估随 P2-1 一并路由、PR+代码文档化、收敛合并 | 本地全绿（control-plane **89 测** vitest〔queue 14 / sweep 28〕+ tsc 0；全仓 Py 397 + redline 25 + schema no-drift 不受影响）+ **CI 5 job 绿** | ✅ 合并 #45（squash `a90a3d4`）。**归 DEPLOY/M2-CLOSE：P2-1 worker 订阅队列（真降延）+ 节流版 stale 重发（需 `last_wake_at` 列，与 worker 订阅同组）**；**归 CFG-GUARD：break-glass 生产锁 cf_queues**。下一就绪：T2.6(#20)·SECRETS(#21)·CFG-GUARD(#22)·FREE-POOL(#23)·OBS(#24)·DEVLOOP(#25) |
| 2026-06-26 | **CFG-GUARD（§14 运行时配置守卫 + 审计 · 红线我亲做）**：D1 `settings` 表真源（+ 每版本快照 `settings_snapshots` + 变更审计 `settings_audit`），KV 热缓存随每次变更重投影；改配恒走守卫层（不裸改 D1）。守卫：每键上下界/枚举（13 键）+ **`Object.hasOwn` 查找**（原型键 constructor/toString/__proto__ 按 unknown 拒）+ 跨键不变量（lease≥2×heartbeat · jobHardTimeout≥lease · jobTtl≥deadlineMaxWait）+ **红线键不可变**（allow_paid/paid_providers/aigc_*→403，CI 专步断言 RED_LINE∩MUTABLE=∅）+ **settingsVersion 原子自增**（in-SQL、并发不塌缩）+ **每版本快照**（`getConfigByVersion` + `/internal/config?version=N`，job 按 settings_version 解析不漂移）+ 审计 who/when/old→new/why（与 setting+版本同 batch 原子、old 子查询）。**独立 ADMIN_TOKEN**（与 worker bearer 分离→worker 被攻陷不能改配；未配 fail-closed 503）。getConfig KV 未命中读穿 D1 真源。AIGC 法定标识非全局运行时开关（按 output_mode 逐 job、红线 3）。**验收全绿**：改 cap 热生效 / 红线键改不动(CI) / 越界+跨键拒 / 快照不漂移 / 审计留痕 / break-glass 留审计 | feat/cfg-guard-settings（impl `7fe27ca`，单 commit 含审修） | #48（squash `110ff6f`） | 对抗多透镜 workflow **5 修**（原型键绕过 / 版本竞态原子化 / 跨键不变量 / 冷 KV 读穿 / dead-code 接线）→ CodeX CLI **R1 2 P1+2 P2**（每版本快照 + 独立 admin 鉴权 + TTL/deadline 不变量 + AIGC 重构注释）→ **R2 2 P2 并发**（仅多 admin 并发可触发、单 operator 顺序写无竞态、完整修=DO 单写者 → 路由 DEPLOY，§7 停）+ @codex bot 终审 **无 findings**（COMMENTED on `7fe27ca`） | 本地全绿（control-plane **vitest 129**〔settings 26 + guard 3〕+ tsc 0；worker 不受影响）+ **CI 5 job 绿**（含 §14 红线不可变专步） | ✅ 合并 #48（squash `110ff6f`）。**归 DEPLOY/M2-CLOSE：并发多 admin 写串行化（DO 单写者，与多操作员身份同落）+ worker 按 job 版本请求 `/internal/config?version` 接线**；**归项目主：AIGC 审计化全局禁用机制（合规敏感、红线 3，不自主引入）**。**前置(用户)：`ADMIN_TOKEN` 注入 CF secrets（与 INTERNAL_TOKEN 分离）**。下一就绪：FREE-POOL(#23)·OBS(#24)·DEVLOOP(#25) |
| 2026-06-26 | **SECRETS（worker 凭据拉取 + 密钥轮换 · 决策 B · 红线我亲做）**：worker 箱**只放 bootstrap 共享密钥**；R2 存储凭据 + 免费 provider key（Groq/CF/DeepL）启动经 `/internal/credentials`（**TLS+共享密钥认证**）拉取、**仅内存持有**，箱盘无 provider key。本单元开 T2.1 的 credentials inert stub（`501`→真凭据）：`collectCredentials`（r2 + 已配 providers；**未配存储 fail-closed 503**；provider 半配则省略；密钥永不入日志/错误体/URL）。**零停机轮换**：control-plane `requireWorker` 接受 `INTERNAL_TOKEN`(current) **或** `INTERNAL_TOKEN_NEXT`(next)〔常量时间、两次比较都执行〕；worker 端 401 回退 staged next 并 **CAS 提升**（锁保护快照 + 幂等 CAS，**心跳线程 vs 主线程并发**不污染 `_auth`→None）；`require_secure_base` 拉凭据前**强制 HTTPS**（窄 localhost 例外给 DEVLOOP）。`__main__` 启动拉 creds 建 S3Storage，箱 env 仅 bootstrap 密钥。**验收全绿**：箱盘无 provider key / 轮换零停机 / 鉴权拒未授权(401·503) / 仅内存 | feat/secrets-worker-creds（impl `3ef5087`，单 commit 含审修） | #47（squash `49d84cd`） | 对抗多透镜 workflow（5 镜→验真 **1 P3**：parse 对 null/空 r2 字段 fail-loud）+ CodeX CLI **R1 P1**（双密钥提升线程安全：心跳 vs 主线程→锁快照+CAS）→**R2 P2**（拉凭据前 TLS 强制）→**R3 clean**（descending、互异）+ @codex bot 终审 **clean**（"Didn't find any major issues 👍"，`3ef5087`） | 本地全绿（control-plane **vitest 100**〔+11 credentials；stale 501 测试改 503〕+ tsc 0；worker **ruff/pyright/pytest 57**〔+10 credentials：解析/内存-only/双密钥线程安全/TLS〕）+ **CI 5 job 绿** | ✅ 合并 #47（squash `49d84cd`）。**归 FREE-POOL：provider 适配器消费 `WorkerCredentials.providers`**；**归 DEPLOY：provider key / Turnstile 注入 CF secrets（不经我）+ worker 部署 bootstrap 密钥双键轮换 runbook（set next→滚 worker→promote→drop old）**。**re-route（T2.6 曾归 SECRETS）：anon_id 服务端 HMAC 签名/验证 → 归 M2-CLOSE**（控制面身份完整性，与双池 anon cap 同组；SECRETS 已立 server-secret 注入模式可载 HMAC key——非 SECRETS backlog §126-129 worker-creds scope）。下一就绪：CFG-GUARD(#22)·FREE-POOL(#23)·OBS(#24)·DEVLOOP(#25) |
| 2026-06-25 | **T2.4（abuse gate 骨架 + presign/SSRF CI · 红线邻近我亲做）**：①**worker ffprobe 复核**（T2.1 归来项「上传对象不可变」）claim 后对**真实下载字节**重核——**HEAD 大小预检**(Content-Length)拒超限于缓冲前 + **下载按 `max_upload_bytes` 界读**（至多 cap+1 字节，闭合 HEAD→GET swap 竞态、内存恒界）+ 格式/SSRF allowlist(复用 T1.3c) + **per-mode 时长 cap**（§13；`both` 独立读不塌缩 dub），违规 **fail+删源**（先报后删、报成功才删→瞬时 fail 下保留源、重排幂等重拒）·②**abuse gate 骨架** Turnstile(配 `TURNSTILE_SECRET_KEY` 即 fail-closed、否则惰性桩；先于 `verifyUpload` 故失败挑战不消耗 session)+per-IP /64 key，双池原子计数留 M2-CLOSE·③**SSRF/presign 守卫 CI**(新 job)按函数体作用域校验：无 yt-dlp + ffmpeg file,crypto+格式 allowlist + 复核调用先于产物/HEAD 先于下载/下载有界 + presign HEAD 先于建 job·短 PUT·派生 key·download() 归属过期清除·④**nftables egress 雏形**：默认 drop·封 IMDS+RFC1918+IPv6·仅 CP/R2/provider 经命名集·作用域化重置(不 flush 全表)、DNS 刷新留 DEPLOY | feat/t2.4-abuse-ssrf（impl `481348f` + 审修 `fc3acca`/`3d1b9d8`/`dac2f0c`/`e497dd0`/`41b02ef`/`73169c5`） | #44（squash `354ead4`） | 对抗多透镜 workflow（6 确认全修）+ CodeX CLI **5 轮 descending**：R1 P1（下载前未限大小 OOM）→R2 P2（HEAD→GET swap 竞态）→R3 P1（Turnstile 绑定名 `TURNSTILE_SECRET_KEY` 静默失效）→R4 2×P2（守卫谓词假阴性→按函数体作用域化）→R5 2×P2（nftables 不 flush 全表 + 守卫严格匹配 accept）；**运行时自 R3 收敛**、§7 停 CLI → @codex bot 终审 **1 advisory P2**（worker 独立尊重 per-mode `both` cap）已修 | 本地全绿（control-plane **71 测** vitest〔abuse 7〕+ tsc 0；全仓 Py **397 测**〔admission 8/worker 22/guardrail 10/ffmpeg-ssrf〕+ ruff + pyright 0 + redline 25 + schema no-drift）+ **CI 5 job 绿**（新增 SSRF/presign 守卫） | ✅ 合并 #44（squash `354ead4`）；**闭合 T2.1 上传 swap TOCTOU 端到端**。**归 M2-CLOSE：双池 cap 原子计数** + **`worker_lost` 配额退还**（双池 counters 表）；**归 DEPLOY：egress DNS 刷新活规则**（不漏新 IP，与本单元 SSRF CI 一致）。下一就绪：T2.5(#19)·T2.6(#20)·SECRETS(#21)·CFG-GUARD(#22)·FREE-POOL(#23)·OBS(#24)·DEVLOOP(#25) |
| 2026-06-27 | **FREE-POOL（免费池状态 + provider circuit-breaker · 红线我亲做）**：免费 provider 429/配额耗尽的**共享熔断器**——一箱见 429，全箱不复撞，到点自动恢复，**绝不因此转付费 API**（红线 §1/§14）。**seam**：状态归 control-plane（D1/KV），路由归 provider-adapters（纯函数·吃快照吐结果·**不 import control-plane/D1/KV**，AST CI 边界 lint）。**provider-adapters `circuit.py`**（纯）：`route_free(kind, snapshot, configured?)` 跳过熔断 provider（429 不复撞）+ `now`-相对 reset 自动恢复 + 全耗尽→`free_pool_exhausted`（只走 $0 AUTO_LADDER + paid 防御跳过=**绝不返回 paid**）+ `configured_free_providers`（纯消费 `WorkerCredentials.providers` **名**、secrets-safe = SECRETS→FREE-POOL 闭合）。**control-plane `providers.ts`**：`provider_quota`（migration 0004，**D1 权威**）+ KV `provider_availability` **fail-soft 镜像**；`GET /internal/providers/availability`（**D1 权威优先读**〔避免多 box 并发 KV 全量快照乱序覆盖丢熔断态〕+ now 过滤=自动恢复，字段 `now_ms`/`exhausted_until` 与 dataclass 对齐）+ `POST /internal/providers/exhausted`（worker 鉴权；**paid 名→403 `forbidden_provider`** + unknown 400 + resetAt 非未来 400 + **>7d clamp 不 drop**〔DeepL 月配额仍熔断、不被拒、worker 再探续报〕）；UPSERT `exhausted_until = MAX(stored, incoming)` **熔断窗口单调**（短 reset 后到不缩短）。`FREE∩PAID=∅` 守 + CI 专步。**AssemblyAI 保持 PAID 不入免费池**（一次性额度后计费，纠正业主"ASR兜底"误解，仅 Tier-2 BYOK 显式授权可用）。**验收全绿**：429 不复撞 / reset 自动恢复 / 全耗尽 free_pool_exhausted / 绝不转 PAID / 边界 lint | feat/free-pool（impl `76be169` + 审修 `bfba19e`/`4385e45`/`b76a9d9`） | #49（squash `4cfb999`） | 对抗多透镜 workflow（5 镜→验真 **1 P3**：熔断窗口非单调→MAX on conflict + 测）+ CodeX CLI **R1 3 P1**（**并发 KV 陈旧覆盖**→读路径改 D1 权威优先 + KV best-effort；ruff I001 导入序 + E501〔此前本地 ruff 因 `cd` 残留跑错目录假绿、被 CLI 抓出〕）→**R2 clean**（descending）+ @codex bot 终审 **R1 2 P2**（快照字段跨语言对齐 `now_ms`/`exhausted_until` + 超上限 reset clamp 不 drop）全修→inline 全 outdated、CI 绿 = §7 收敛 | 本地全绿（control-plane **vitest 142**〔providers 10 + guard 3〕+ tsc 0；provider-adapters **pytest 86**〔circuit 14 + seam 2〕+ ruff/pyright 0 + redline 29）+ **CI 5 job 绿**（含 §23 红线 FREE∩PAID 专步） | ✅ 合并 #49（squash `4cfb999`）。**归 M2-CLOSE：worker `route_free` 调用点接线（真管线）+ 全局分钟池联动原子计数 + 凭据→adapter 活注入（worker 把内存 creds 喂给真实 adapter 调用）**。下一就绪：OBS(#24)·DEVLOOP(#25) |
| 2026-06-27 | **OBS（可观测性基线 + 告警 + worker /progress 遥测 · 补 §12 缺口）**：结构化 JSON 日志（`obs.ts` `logEvent`/`buildLogLine`，**allowlist 非 denylist**——键名 denylist 堵不住开放值空间）keyed by job_id；**派生指标读视图** `computeMetrics`（job 计数 · claim 时延〔**按 started_at 开窗**，暴露排队超窗后才认领的饥饿样本〕· 阶段耗时 · 免费池余额〔configured_total 取 env 已配 key-gated 免费 provider {groq/cloudflare/deepl} ∩ 熔断、比值 ∈[0,1]〕· 全局分钟 gauge〔best-effort SUM(advisory)，权威原子计数归 M2-CLOSE〕· worker liveness〔精确 `last_lease_expires_at` + 明命名估计 `last_heartbeat_estimate_at`〕）+ **≥2 告警** `evaluateAlerts`（`lease_overdue` running 超租约 · `free_pool_low` 免费池耗尽逼近，configured≥2 floor 避单 provider 例行 429 误报）；`GET /internal/admin/metrics`（**admin 鉴权 operator-only、刻意非 worker 可拉**）。migration 0005 `progress_meta`。**脱敏三处皆 allowlist**：日志（闭键集 + stage/provider **闭枚举值**校验）+ metrics payload（仅聚合，无 anon/upload_session/artifact key/error_detail）+ 存储 progress_meta（`parseProgressMeta` 六字段 allowlist over 原始体）；heartbeat **stage 闭枚举** `isKnownStage` + **provider 闭名集** `isKnownProvider`（slug 形 token 如 `gsk_…` 拒）+ **未知 stage 丢弃不 400、租约照常续**（stage 词表绝不 gate 租约续期→无失联 footgun）+ **reclaim/requeue 清 progress_meta**（不把死尝试遥测算进新 claim）。worker `ProgressTelemetry`（仅名+整数、结构上不可含 key/IP/文件名）+ `build_progress_body` + Heartbeat 上报 `stage_elapsed_ms`（桩无 provider 选择→provider/chunk/free_pool_result 留空至 M2-CLOSE）。**验收全绿**：指标可查含 worker 阶段数据 / 2 告警可触发 / 脱敏断言（日志+payload grep 无敏感字段） | feat/obs（impl `39b65d3` + 审修 `e628585`/`f76cd6d`/`177a82a`） | #50（squash `6af5416`） | 设计评审（**开工前** 3-lens workflow→3×ready-with-fixes、P1 全折叠：current_stage 注入闭合·logEvent allowlist·free-pool 分母取 env）+ 对抗多透镜 workflow（4 镜 redaction/correctness/scope-seam/fail-closed→验真 **0 confirmed**，1 uncertain P3 租约 gauge config 耦合已去耦）+ CodeX CLI **R1 2**（P2 `provider` 闭名集〔slug 形 `gsk_…` token 泄漏〕· P3 数值 `status` 日志保留）+ @codex bot **R1 1 P2**（reclaim 清陈旧遥测）→**R2 2 P2**（`stage` 闭枚举 · claim 时延按 `started_at` 开窗）→**R3 0**（无新 inline、CI 绿）= §7 收敛 | 本地全绿（control-plane **vitest 166**〔obs 24〕+ tsc 0；全仓 Py **430** + redline 29 + pyright 0 + ruff 净；worker 不受影响） + **CI 5 job 绿**（含 §24 脱敏不变量专步） | ✅ 合并 #50（squash `6af5416`）。**归 M2-CLOSE：全局 job/分钟原子计数 + cap + 成本逼近 cap 告警（无计数表 / config 无全局 cap）+ worker `route_free` 接线后 provider/chunk/free_pool_result 遥测真值（桩 worker 无 provider 选择，契约先落值后填）+ 真 `lease_renewed_at` 列（如需精确 worker 末次心跳、去 leaseTtlMs 估计耦合）**。下一就绪：DEVLOOP(#25) |
| 2026-06-27 | **DEVLOOP（本地 dev loop · 补 §15 缺口，非阻断）**：`just dev` 一条命令本地端到端 upload→claim→complete，不依赖云。**数据面 seam（prod-safe，env 不设时字节一致）**：control-plane `R2_S3_ENDPOINT`（`presignR2Url` 上传/下载 + `verifyUpload` HEAD/delete 经新 `media.ts`）+ worker `OVT_R2_ENDPOINT`（`S3Storage` 路径式签名）→ 指向本地 S3 stub；缺省走真 R2 host / `MEDIA` binding。非 client 可控、operator 部署配置、worker egress allowlist 兜底 → 无 SSRF 面。**新增（均在 `dev/`、不入 hosted bundle）**：`dev/local_s3.py`（stdlib 内存 S3 stub、仅本地无鉴权）+ `apps/control-plane/dev/cp_server.ts`（复用真 `handle()` 的本地 HTTP 服务，better-sqlite3 D1 + 内存 KV，经 `tsx`；**刻意无 wrangler 依赖**——仓库一贯 better-sqlite3 模拟 D1、跨进程并发已 T2.0 硬门证；刻意不入 Worker tsconfig：Node+workers-types 全局冲突，tsx 运行 + `just dev` 行为验证）+ `dev/dev_loop.py`=`just dev`（起 stub+CP+真 worker 驱动一单 + 校验产物字节回环；缺 ffmpeg/node/pnpm loud SKIP）。worker ffprobe 准入（T2.4）不绕、喂真 1s mp4。 | feat/devloop（impl `5697218` + 审修 `5c58b08`/`ebb4d61`/`080cbb1`） | #51（squash `6d82a99`） | 对抗多透镜 workflow（4 镜 redline-prodsafety/security-ssrf/correctness-fidelity/scope-tests → **0 confirmed**；1 P3 cp_server 不入 tsconfig→skeptic 驳 real=false + 注释加固）+ CodeX CLI **clean**（确认 override 不设时 prod 路径不变）+ @codex bot **R1 1 P2**（fresh checkout `just dev` 仅 `uv run` 不装 workspace 成员→worker `import media_worker` 失败、空等超时）→**R2 2 P2**（同主题：TS 侧 tsx/better-sqlite3 deps 未装 + worker 启动依赖 ambient env）→**R3 0**（"Didn't find any major issues 🚀"）= §7 收敛（descending、均 fresh-checkout 真 bug） | 本地全绿（control-plane **vitest 172**〔devloop 6：presign override + media.ts 双分支〕+ tsc 0；全仓 Py **435**〔devloop_storage 5：S3Storage override + 真 SigV4 round-trip vs stub〕 + ruff/pyright（含 `dev/`）0 + redline 29 + SSRF/presign 守卫 + schema no-drift）+ **真跑 `just dev` PASS**（3992B 字节回环）+ **CI 5 job 绿**。修：`just dev` 依赖 install-ts+install-py（fresh checkout 装齐两栈）+ worker 经 `uv run --package media-worker` 自包含启动 + orchestrator worker 早退快失败 | ✅ 合并 #51（squash `6d82a99`）。**M2 非里程碑单元全部完成**（T2.0–T2.6 + SECRETS + CFG-GUARD + FREE-POOL + OBS + DEVLOOP）→ 下一 = **M2-CLOSE(#26)** 里程碑边界（待项目主 sign-off + 真凭据/基础设施）。 |
| 2026-06-28 | **M2-CLOSE PR-A（worker 跑真 autodub-core 管线 + FREE-POOL 路由 · #26 三 PR 之一/3）**：替换 T2.2 桩 worker → 真内核管线。`inject_provider_env`（SECRETS 拉取的免费 key 折进进程 env，仅名记日志）；`run_real_pipeline` 每段 ASR/MT/TTS 经 `route_free`（**只走 $0 ladder、绝不返回 paid**）覆盖 `job.plan` → 跑内核 → **失败重路由**：429（`QuotaExhausted`）= 上报熔断 + **provider 级**排除（不复撞）；输入拒绝（`ProviderUnavailable`，**含 adapters 与内核两个类**）= **仅本 stage 本地排除、不上报**（健康 provider 不全局熔断）；**路由期资格过滤**（TTS commercial-safe + piper-locale；MT 对 DeepL 不支持目标剔除）；**惰性 fail-closed 哨兵**（无语音任务跳过 MT/TTS）；`LanguageError`/`FreePoolExhausted` → `free_pool_exhausted`/`unsupported_language_pair`；内核保持 frozen（`allow_paid` 恒 False，不 import gateway/billing）。 | feat/m2-close（impl `0441aff` + 审修 `6da9dc9`/`2750110`/`9ab1120`/`c5bc8be`/`085f421`/`8fbb242`） | #52（squash `4f39c5d`） | 对抗多透镜 workflow（pre-PR）+ CodeX CLI R1–R3（R4 撞用量限）+ **@codex bot 7 轮**：R1 2 P2（快照刷新 blip→复用旧快照 · Piper 错语种 voices_for fail-closed + 路由过滤）→R2 1 P2（无语音预路由误拒→惰性哨兵 resolver）→R3 1 **P1**（**回归**：resolver select kw-only allow_paid vs pin_resolver 位置转发→镜像内核签名 + pin_resolver 回归测）→R4 1 **P1**（无 hint CF MT→internal_error→ProviderUnavailable 也触发**本地**重路由、不上报）→R5 1 **P1**（**回归**：共用 helper provider 级排除漏 scope→`provider_wide` 参数化 + 单 stage）→R6 1 P2（MT 没按目标过滤 DeepL→路由期过滤）→**R7 clean "Didn't find any major issues"**。额外：**重路由路径 6-lens 对抗自审**（5 清 + 1 潜在 P3：内核 ProviderUnavailable 类绑定→两类都接）+ **资格扫描**（确认 DeepL 是唯一缺口）。 | 本地全绿（全仓 Py **474** + redline 29 + guardrail 10 + ruff/pyright 0）+ **CI 5 job 绿**（Py/redline/ssrf/schema/TS） | ✅ 合并 #52（squash `4f39c5d`）。**#26 未闭**（M2-CLOSE = 3 PR；PR-A=1/3）。下一 = **PR-B（双池 cap daily_counters + per-IP/anon/user + 全局 job/分钟原子预留 + worker_lost 幂等退还 + anon_id 服务端 HMAC + cap 逼近告警）** |
| 2026-06-29 | **M2-CLOSE PR-B（双池 daily cap + worker_lost 退还 + anon_id 服务端 HMAC + cap 逼近告警 · #26 三 PR 之二/3）**：控制面单元（仅 `apps/control-plane`，不触 Python/autodub-core/schema）。**双池 cap**（migration 0006 `daily_counters`，global/actor/ip 按 UTC-day；`reserveDualPool` 每池 SEED + 守卫 UPDATE〔jobs+1<=cap AND minutes_ms+add<=cap〕单 batch + 部分预留补偿 → 超 cap **429 `daily_cap_reached`**；GLOBAL 池 = 绝对成本天花板红线§1；reserve 在 verifyUpload **之前**〔cap-reject 不烧会话〕，坏上传**用户错计数/我方错补偿**）·**幂等退还**（`refundLostJobs` sweeper 职责，覆盖 OUR-fault 终态集 `REFUNDABLE_ERROR_CODES`=worker_lost/internal_error/processing_timeout/free_pool_exhausted/source_fetch_failed；守 refunded=0 的 decrement + flip **同一事务 batch**〔原子·幂等·崩溃回滚重试〕；`reserved_minutes_ms` 建时按**模式硬 cap**快照，ungameable）·**anon_id HMAC**（`anon.ts` mint `POST /api/anon` + `getActor` 验签于 **BASE id**〔密钥轮换不孤儿化〕；`ANON_ID_HMAC_KEY` 未设：仅 `OVT_ENV=dev` 收裸 id、缺省/prod **fail-closed 503**；轮换 overlap 认 `ANON_ID_HMAC_KEY_PREVIOUS`）·**cap 逼近告警**（obs `global_cap_approaching` 读当天 global 桶 + cap）·cap 旋钮 MUTABLE 非红线。 | feat/m2-close-prb（impl `949bf3e` + 审修 `7c1ab54`/`d86f0eb`/`deb4eca`/`392f97f`/`f0a0c49`/`9685d76`/`f0f9925`） | #53（squash `9559bfc`） | **开工前设计评审 workflow**（5 镜+skeptic，15 confirmed → 折入 5 P2 设计修正 + 10 P3）+ **实现对抗审 workflow**（6 镜+skeptic → 2 P3：unauth-mint=by-design / FC-1 compensation 过包 post-commit 已修）+ **CodeX CLI R1–R5 descending**（R1 3P1+1P2〔advisory 分钟/prod fail-open/退用户错/SPA 断〕→R2 1P1+1P2〔infra 补偿/refund 原子〕→R3 1deferral-P1+2P2〔our-fault 退还集/missing-object 复用/config-version〕→R4 1P2〔missing-object expire 并发竞态〕→**R5 窄修**〔统一三坏上传守卫 expire + HMAC 轮换 previous-key〕）+ **@codex bot**（P1 SPA-mint=go-live defer / P2 refund 已修）。 | 本地全绿（control-plane **vitest 210** + tsc 0；全仓 Py 不受影响）+ **CI 5 job 绿**（TS/Py/redline/ssrf/schema）+ MERGEABLE/CLEAN | ✅ 合并 #53（squash `9559bfc`）。**#26 未闭**（PR-B=2/3）。**归延（业主确认 2026-06-29）：①SPA 调 `POST /api/anon` 取签名 id 接线 = GO-LIVE BLOCKER**〔apps/web/T2.6；接线前**不注入 `ANON_ID_HMAC_KEY`、不开公网生产流量**；fail-closed 非不安全〕**②worker 按 job settings_version admit**〔精确分钟强制，对账 operator 中途升 cap；既有 M2-CLOSE/DEPLOY 归延〕③精确按 ffprobe 实时长扣分钟对账 → PR-C。下一 = **PR-C（lease_renewed_at + deadline comparator + deadline_exceeded + 错误码注册表收口 + 2-并发 soak + §12 DoD 全门，closes #26）** |
| 2026-06-30 | **M2-CLOSE PR-C（deadline-aware comparator + deadline_exceeded + 错误码注册表收口 + §12 DoD 门 · #26 四 PR 之三/4，control-plane 收口·不闭 #26）**：仅 `apps/control-plane` + `packages/schemas`（不触 worker/autodub-core）。**deadline-aware comparator**（`claim.ts`：`deadline_at = enqueue + deadlineMaxWaitMs(4h)` 跨模式反饿死硬-SLA；overdue〔`deadline_at<=now`〕提升至 output_mode mode-tier **之上**、overdue 间按 deadline_at 升序；CLAIM_SQL ORDER BY 镜像 `compareClaimable` 完全一致，claimParams 11 参）·**`enforceDeadlines` 扫描职责**（`sweep.ts`：终结**任何**非终态、非活跃进行、past-deadline job〔`queued` 或 `running`+租约过期〕为 `failed`/`deadline_exceeded`；**先于 recoverLeases 跑** → 每 job 恰一职责处理〔无 requeue→terminalize 抖动/无双计〕；live-lease running 不动〔worker 自身 jobHardTimeoutMs 兜底〕；**可退还**＝无产出退用户日 cap，**同 worker_lost** 规则）·**错误码注册表收口**（`errors.ts` 单一真源，编译期钉冻结 ErrorCode 契约〔`satisfies` + `_MissingFromRegistry` 双向穷尽守〕；`REFUNDABLE_ERROR_CODES` + `WORKER_REPORTABLE_ERROR_CODES`=注册表 **排除 CP-sweeper-only**〔worker_lost/deadline_exceeded〕= `/fail` allowlist；caps.ts/jobs.ts 改 import 此处）·`deadline_exceeded` 经 schema 源 + codegen 入契约〔幂等无漂移；并修 codegen 顶层 Literal 换行 off-by-one ruff E501〕·**§12 DoD meta-gate**（15 桶；调度桶标 `partial → PR-D`〔`free_min_share` 预留槽位拆出〕，非静默绿）。 | feat/m2-close-prc（impl `2b55788` + 审修 `17dcfd3`/`421c036`/`c124999`） | #54（squash `eff9005`） | **6-lens 对抗自审 workflow**（16 finding → 验真 **0 real**）+ **CodeX CLI R1–R3 descending**（R1 1×P2〔deadline 作用域：enforceDeadlines 误终结刚 requeue 的重试 job〕→R2 2×P2〔deadline_exceeded 不该在 /fail allowlist〔worker 自报可退还码〕→加 WORKER_REPORTABLE；R1 的 started_at 限制把 requeued job 永卡→反转为硬-SLA 覆盖 queued+lease-expired-running〕→**R3 clean**）+ **@codex bot 终审 clean**（"Didn't find any major issues"，`c124999`）。 | 本地全绿（control-plane **vitest 244**〔deadline/errors/soak/dod 新增〕+ tsc 0；packages/schemas ruff/pyright/pytest 11 + codegen 幂等无漂移）+ **CI 5 job 绿**（TS/Py/redline/ssrf/codegen-diff）+ MERGEABLE/CLEAN | ✅ 合并 #54（squash `eff9005`）。**#26 未闭**（PR-C=3/4，control-plane 收口）。**`lease_renewed_at` 已弃**（无消费者；heartbeat 已用 `lease_expires_at` 编码末次续租）。下一 = **PR-D（worker 并发≤`worker_concurrency`=2 + `light_slot_reserve`≥1 轻槽预留 `free_min_share`，test-first Python media-worker，**closes #26**=M2-CLOSE 里程碑边界，待项目主 sign-off）** |
| 2026-06-30 | **M2-CLOSE PR-D（worker 并发 ≤ `worker_concurrency` + `light_slot_reserve` 轻槽预留 `free_min_share` · #26 四 PR 之四/4，closes #26 = M2 收口）**：worker 侧并发 + 轻槽预留（plan §6 line178/§8 line212）。**`scheduling.py`（新）**：纯策略 + 线程安全 `SlotTracker`；`weight_class`（subtitle_only→LIGHT，其余/未知→HEAVY fail-safe）、`heavy_budget = max(worker_concurrency − light_slot_reserve, 1)`（钳 ≥1，1-槽 2GB 箱仍跑 dub）、`plan_claim`→NONE/ANY/LIGHT_ONLY。**`worker.py`**：`run_forever` 重构为单 dispatcher + `ThreadPoolExecutor`；仅余预留槽时 claim `light_only`；submit 前由 dispatcher 独占 add 槽、`finally` remove；stop 时排空在飞 job。**`control_plane.py`**：`ControlPlane.claim(*, light_only=False)` 协议 + `HttpControlPlane` 仅 True 时发 `{"light_only": true}`（否则空体=向后兼容）。**`__main__.py`**：`OVT_WORKER_CONCURRENCY`（默认 1，§14 2GB 基准）+ `OVT_LIGHT_SLOT_RESERVE`（默认 1）。**control-plane**：`claim.ts` `ClaimOpts.lightOnly` + 仅-WHERE 候选过滤（`(? = 0 OR output_mode='subtitle_only')`，§8 ORDER BY 不动·comparator/SQL 镜像不变；claimParams 11→12 参）·`core.ts` `optBool`·`jobs.ts` `claimNext` 读可选 `{light_only}` 体（空=false，畸形/非布尔→400 fail-closed）。 | feat/m2-close-prd（impl `176b9fd`） | #55（squash `272cc17`） | **6-lens 对抗自审 workflow**（2 finding → 验真 **1 P3**：run_forever/入口默认值不一致 + plan §6/§14 矛盾 → 入口默认对齐 1〔§14 2GB 基准，旧 2 是过时 Oracle-12GB 值〕避未配 2GB 箱 OOM；1 refuted）+ **CodeX CLI clean**（`--base main`：no actionable issues）+ **@codex bot 终审 clean**（"Didn't find any major issues"，reviewed `176b9fd`）。 | 本地全绿（worker ruff/pyright〔含 tests〕/pytest **491**；control-plane **vitest 250**〔claim_light 新增 6〕+ tsc 0；redline 29 + ssrf/presign + schema 无漂移）+ **CI 5 job 绿**（TS/Py/redline/ssrf/codegen-diff）+ MERGEABLE | ✅ 合并 #55（squash `272cc17`）**closes #26** → **M2-CLOSE（#26）全 4 PR 收口、M2 里程碑完成**。停在 **M2→M2.1 边界**待项目主 sign-off。 |
| 2026-07-01 | **M2.1（fast-follow · 烧字幕端到端 · #27 之一/2，不闭 #27）**：`burned`/`both` 字幕烧录（libass 重编码）内核+worker+控制面+SPA 全链。**autodub-core**：`ff.burn_subtitles`（libass 重编码：宽×高 cap 只下采样+偶数、`yuv420p`+AAC 浏览器兼容、编码超时→FfmpegError〔非付费重试〕、srt 暂存 cwd 防注入、原子写、SSRF 复核）+纯 `_burn_vf`/`probe_dimensions`；`mux()` 烧录分支（burned_video=交付视频、dubbed_video 中间态；per-locale libass 字体由调用方传入、内核不 import 注册表；cache-key 含 cap+字体；burn 关时任何 burn 请求 fail-loud）；`config` `BURN_SUBTITLES_ENABLED`+caps。**worker**：`_deliverables` burned 复用 `video_key`；`weight_class`/调度 burned=HEAVY（不占 free_min_share 轻槽）；admission 拒音频源烧录 + burned 按 dub 档时长 cap。**control-plane**：`complete()` 模式矩阵校验（缺/多 key→400，仅 winning-claim）+ createJob 收 burned + 预留按有效烧录 cap + §8 快道排除 burned；`reservedMinutesForMode` burn-aware。**SPA**：交付选择器（srt/烧录/两者）+ 警示按 subtitle_delivery 算。schema 早含 `burned`→无 codegen 漂移。 | feat/m2.1-burn-subtitles（impl `feb3173`/`eebf14c`/`db7ca01` + 审修 ×8） | #58（squash `3dffacc`） | **6-lens 对抗自审**（2→验真 1 P3）+ **CodeX CLI R2–R4**（yuv420p+AAC 归一 / 偶数尺寸 / flag-off 矩阵…）+ **@codex bot 4 遍**（burned=heavy 调度 + 音频拒烧 + 烧录时长 cap 三层一致〔worker+CP+SPA〕+ §8 优先级排除 burned）= 共 **11 真问题全修、0 P1**；CLI R5+bot 前两遍曾撞 OpenAI 配额（后恢复重跑）。 | 本地全绿（pytest **512** + pyright + ruff；control-plane **vitest 263** + tsc；web svelte-check + **vitest 30**；redline 29 + ssrf + schema 无漂移）+ **CI 5 job 绿** | ✅ 合并 #58（squash `3dffacc`）。**#27 仍 OPEN**（M2.1 烧字幕已并；剩 **目标语广集**〔逐语 `language_capabilities` vet 扩充〕单独 PR 收口 #27）。停在 **M2.1→M3 边界**，M3 待项目主 sign-off。 |

| 2026-07-03 | **Go-live prep（业主 2026-07-02 全权授权部署上线；律师 §9C/AD-14 门默认通过）**：三单元并行推进至 PR+审、VPS 置备。**#60 SPA anon 接线**（P1 GO-LIVE BLOCKER 解除）：`ensureServerAnonId`〔server-mint-first，dev/离线回落本地 id〕 + mint 10s AbortSignal 超时 + 身份获取 UI 态 + 401 `recoverAnonId`〔clear-first、provenance-honest 文案〕 + downloadArtifact 也接 401 自愈。**#61 DEPLOY**：`workers/media-worker/Dockerfile`〔amd64、piper+faster_whisper 构建期 bake+sha256 MANIFEST、piper-tts 1.2.0/faster-whisper 1.0.3 钉版、FVD_PIPER_LANG 语言门、HF_HUB_OFFLINE、非 root〕 + `deploy/docker-compose`〔host-net 承重、restart unless-stopped〕 + egress DNS 刷新守护〔单事务 flush+add、fail-safe、boot-apply service 重启持久〕 + wrangler `assets` 同源 SPA + `.github/workflows/deploy.yml`〔probe/migrate/deploy，用 repo CF token secret〕 + `docs/DEPLOY-RUNBOOK.md`。**#62 M3**：kill-switch〔`servicePaused` mutable 非红线 → uploads/jobs 503〕 + DMCA/DSA takedown〔`POST /internal/admin/takedown` admin 鉴权，清全 attempt 产物+源、终态 `taken_down`、bump cv、幂等；migration 0007〕 + SPA 隐私/条款/下架页。 | feat/spa-anon-mint（`ac8a283`）· feat/deploy（`79bade1`+lint 修）· feat/m3（pushed） | #60 · #61 · #62（均 OPEN，待业主合并） | #60：@codex bot **clean** + CodeX CLI R1–R3〔5×P3〕+ 对抗审〔2 unique P3〕· #61：CodeX 5 + 对抗审 **17 confirmed** 全修〔CF-AI 计费红线 P1 → 仅 Workers Free 计划注入 · egress 重启不持久 P1 → boot-apply service · piper 语言门 P1 → FVD_PIPER_LANG · piper-tts 版本 P1 → 钉 1.2.0 · SIGTERM drain · egress 原子/v6-prune/dedup〕· #62：@codex 已触发、CI 5/5 | #60 CI 5/5 绿 · #62 CI 5/5 绿 · #61 CI 修绿中〔ruff E501 已修〕；全仓本地：Py 512 + worker 119 + CP vitest 269〔+m3 6〕 + web 30/37 + 无 schema 漂移 | ⏳ **未合并**（安全分类器禁 agent 自合并自写 PR，即使业主授权）。VPS（Hetzner CX23 78.46.225.188 amd64/4GB）已置备 docker/ffmpeg/2G swap/入站仅 SSH。**阻断（仅业主）：① 合并 #60→#61→#62 到 main〔#61 带 deploy.yml 上 main 才可分派〕· ② CF 认证〔本机无 token；跑 deploy workflow `probe` 验 secret scope，不足则建新 token〕**。上线步骤见 `docs/GO-LIVE-REPORT.md` + `DEPLOY-RUNBOOK.md`。#28/#29 待对应 PR 合并后闭。 |

| 2026-07-04 | **🚀 GO-LIVE — `openvideotrans.xyz` 公网上线 + 端到端冒烟通过**（业主 2026-07-02/03 全权授权,含域名/VPS/密钥）。**控制面**：CF Worker `ovt-control-plane` 部署到自定义域名（apex,custom_domain 路由自动建 DNS+cert）+ 同源 SPA；接真实资源 `ovt-db`/`ovt-config`/`ovt-artifacts`（迁移 0001–0007 applied）；**owner 密钥走 CF Secrets Store**（`resolveSecrets()` 入口异步 `.get()` 绑 R2 S3 + DeepL → 明文 env）；自生成 `INTERNAL_TOKEN`/`ADMIN_TOKEN`/`ANON_ID_HMAC_KEY` 走 plain wrangler secret（deploy `secrets` 模式）。**VPS worker**（Hetzner CX23 · 78.46.225.188）：docker compose 起 media-worker（镜像 bake piper+faster_whisper）,从 `/internal/credentials` 拉 R2+DeepL,claim 循环。**冒烟**：传含英语语音测试片 → faster_whisper ASR → DeepL 译 → 双语 SRT（含 AIGC 披露 cue）→ 懒签下载,内容正确。**部署途中就地修复（均走 PR）**：pnpm 版本(#63)·资源命名映射(#64)·custom_domain 路由(#65)·CF Queues 移除〔付费,d1 后端〕(#66)·CF Cron→GH Actions sweep.yml 每 5min(#67)·Secrets Store 适配(#68)·worker User-Agent〔CF 边缘 403 挡 Python-urllib〕(#69)。 | main（#60–#69 全并） | #60–#69 | 部署期审计在各单元已做;go-live 修复为运维接线 | `openvideotrans.xyz` HTTP 200 + `/api/anon` 签名 + 上传/建任务/下载全通 + 真实双语字幕 + AIGC 标识 | ✅ **DEPLOY(#28) + M3(#29) 代码面全落地并上线**。**post-launch 待办（非阻断）**：① egress nftables 收口（维护窗口验证 allowlist 含 CP/R2/DeepL）② Turnstile 启用（widget hostname→openvideotrans.xyz + 注 secret + site key）③ 无语音上传→`internal_error` 优雅化（内核边界）④ CF AI/Groq 可选补强⑤ 律师 §9C（业主已定默认通过）。 |

| 2026-07-04 | **Post-launch 修复（前端 + 两个真实浏览器 bug）**：**#71** SPA 重设计（克制开源/CLI 气质:墨迹配色 + 等宽标题标签 + 单一墨绿强调 + 发丝边框 + 深色模式;无渐变/玻璃/emoji;逻辑不变）。**#72/#73** R2 桶 CORS——浏览器直传 R2 是跨域 PUT,桶原无 CORS → preflight 403 → 上传"网络错误"(curl 冒烟不受 CORS 约束未暴露);加 `deploy/cloudflare/r2-cors.json`(GET/PUT/HEAD,origin=站点)+ deploy `cors` 模式。**#75 P1 ApiClient fetch Illegal-invocation**——`this.fetchFn(...)` 以 `this=ApiClient` 调原生 fetch → `TypeError: Illegal invocation`,**真实浏览器里所有 /api(sign/create/poll/download)全废**;单测注入 mock 故绿、curl 不走 SPA、`/api/anon` 因 mint 裸调用幸免。修:默认 fetchFn 改包装箭头 `(input,init)=>fetch(input,init)`。**#74** runbook 补 R2 CORS 步 + 真实资源名。 | main（#71/#72/#73/#74/#75） | #71–#75 | **headless Chromium 真机复现定位 + 验证** | web vitest 39;真机浏览器:anon→sign 200→R2 PUT 200→createJob 201→poll→**完成**(语音片出双语字幕) | ✅ 上线后可用性闭环——`openvideotrans.xyz` 浏览器端上传→翻译→下载全通。**遗留（非阻断）**:无语音上传→`internal_error` 优雅化(内核边界)、Turnstile 启用、egress nftables 收口。 |

| 2026-07-04 | **后台管理台（运维配置 UI · post-launch 增强，非 backlog 单元，业主直接需求）**：给既有 CFG-GUARD 运行时配置补一个界面——此前只能 `curl` 调，现同源 `/admin.html` 可视化设置**上传大小 / 三档时长 cap + 全部运维可调项**（每日额度 global/actor/ip、各类 TTL/超时、`queueBackend`、`servicePaused` 急停开关）。**control-plane**：新增 `GET /internal/admin/settings`（admin 鉴权只读，返回 live 配置 + `mutableKeys` + `redLineKeys`，页面按服务端真源渲染可改/锁定；无写/无 bump/无审计）。**web**：`apps/web/public/admin.html` 自包含（内联 CSS/JS、无打包、CSP 安全、Vite 原样拷入 dist）——粘贴 `ADMIN_TOKEN`（仅 sessionStorage）后单位友好字段（MB/分钟/秒）+ 脏值高亮 + 逐项改动带审计原因 + 审计表 + metrics 快照。**红线 3**：AIGC 标识保持**锁定只读**——`aigc_*` 仍在 `RED_LINE_KEYS`（403）、CI 断言永不可成运行时开关；页面明示不可关，不提供开关（刻意）。 | feat/admin-console（squash `e9d8eb1`, #77） | #77（已合并） | **@codex bot 13 轮迭代收敛**（每轮 1 个 P2/P3 全修：耦合项多趟保存 → 管理读走 D1 非 KV → 时长对象按 fresh 合并 + 终端错误保留未存改动 → reload 失败不僵死 UI → 不重标已应用键 → reload 失败只补已触碰模式 → reapply 只触碰模式 → 操作者身份入审计〔P3〕→ 非 ASCII 操作者名 percent-encode → 清凭证/失败鉴权同清 actor）；并修 main 自 go-live 起 CI 红的 E402（`control_plane.py`）| control-plane **vitest 282**（GET 路由鉴权/返回结构 + KV-stale→D1-read + actor decode）+ tsc 0；**headless Chromium 9 场景**（单位往返 / 多趟耦合保存 / 时长 fresh 合并 / 终端错误保边+报尾 / reload 双故障不僵死 / 已应用键不重标 / 触碰模式粒度 / 张三 actor 不抛 / 清凭证清 actor）；CI 5/5 | ✅ **已合并 `e9d8eb1` + 部署上线 + 真机验证**：`ADMIN_TOKEN` 已轮换（新值交业主）；`https://openvideotrans.xyz/admin`（`/admin.html` 307→`/admin`）headless 真机：粘贴 token → 渲染 live 配置（500MB / 30·5·5min / d1 / v1）+ AIGC 锁定卡 + 操作者入审计 + 无 console error；`GET /internal/admin/settings` 新 token 200、无/错 token 401。 |

| 2026-07-04 | **AIGC 标识可配置 · PR-1/2（字幕通道 · 业主授权 §3 重配，业主自担法律风险）**：业主明确授权把 AIGC 合成标识做成管理台可调（设计本就把此"审计化 AIGC 开关"归项目主，非 agent 自主破线）。分两步：**本 = PR-1**（字幕开关 + 自定义文案 + 全套配置管线 + 解锁冻结红线守卫）；PR-2 = 视频可视水印。**红线守卫收窄**：`RED_LINE_KEYS` 仅剩 `allow_paid`/`paid_providers`（付费 §1 硬锁不动）；`aigcSubtitleEnabled`/`aigcSubtitleText` 入 `MUTABLE_SETTINGS`（默认开、经 CFG-GUARD 审计=acknowledgment）。**schema** `AigcMarking` 加 `subtitle_enabled`/`subtitle_text`（codegen 重生、默认值向后兼容）；`jobs.ts` 按创建时配置烧入（非漂移）。**kernel** `aigc.subtitle_disclosure`/`embed_method`/`metadata_args` 按 `subtitle_enabled` 门控 + 自定义文案（含审计不虚报 + mux 缓存键折入 cue 防陈旧 SRT）。**admin UI** 锁定卡→可编辑「AIGC·字幕」组（开关+文案+合规提示）。 | feat/aigc-config-subtitle（squash `ce98b02`, #78） | #78（已合并） | **@codex bot 3 轮 P2 全修**（embed_method 未随 subtitle_enabled 门控→审计虚报；mux 缓存键漏字幕 cue→陈旧 SRT；初始门控）| control-plane **vitest 280** + tsc；autodub-core **pytest 225**（新增字幕门控/虚报/缓存失效用例）+ ruff + pyright；schemas codegen 无漂移 + worker 119；**headless 管理台 10 场景**（AIGC 开关+文案 POST）；CI 5/5 | ✅ **已合并 `ce98b02` + 部署上线 + 真机验证**。部署顺序（关键）：**先重建 VPS worker**（薄补丁镜像 `FROM ovt/media-worker:latest` + COPY 3 文件——可编辑安装，不重烤 2.4G 模型；避免旧 worker `extra=forbid` 拒收带新字段的 job），后 deploy 控制面。真机 `/admin`：AIGC 组默认开 + 文案 `本字幕由机器翻译生成` + 合规提示 + 无锁定卡 + 无 console error；live 配置 `aigcSubtitle*` 可调、`redLineKeys` 仅剩付费。**PR-2（视频可视水印：位置/大小/颜色/透明度，新 ffmpeg drawtext 叠加）待做**（另开 · 会同样需 worker 重建）。 |
| 2026-07-04 | **AIGC 标识可配置 · PR-2（视频可视水印 · 业主授权 §3 重配，业主自担法律风险）**：承接 PR-1（#78 字幕通道）。业主授权做**视频可视水印**——管理台可调 开关/文案/位置/大小/颜色/透明度，ffmpeg `drawtext` 叠加。**默认关**（业主可翻）：容器元数据法定标识对已标记配音**恒开、与本开关无关**，故关水印**不去任何法定标识**——开启只是更强的"可见"形式；默认开会强制每条配音重编码 + 抢先 M3 保留的"显式可见形态"评审，故 opt-in。启停经 CFG-GUARD 审计。**schema** `AigcMarking` 加 `video_watermark_{enabled,text,position,font_size,opacity,color}`（codegen 重生、默认值向后兼容→worker 先行）。**kernel** `aigc.video_watermark_text` 策略门 + `ffmpeg_utils` 加 `Watermark` + 纯 `drawtext` 构建器（`textfile=`+`expansion=none`→任意 CJK/引号/% 字面绘制、无 filtergraph 转义）+ `watermark_video`（第二趟 x264、音频 stream-copy、`-map_metadata 0` 带容器标识）；烧字幕任务水印**搭同一趟重编码**（`_burn_vf`），纯配音走第二趟；**mux stream-copy 路径不动**，缓存键仅在水印生效时加 `wm:` token（默认关键字节不变）。**§3 审计** run_pipeline 记 `embed_method() or "visible_watermark"`——字幕披露关+水印开的 subtitle_only+burned 任务记 `visible_watermark` 而非自相矛盾的 None。**CFG-GUARD** 6 新 MUTABLE+审计项（颜色 `#RRGGBB` hexColorBound），`RED_LINE_KEYS` 不变（仅付费）；jobs.ts 按创建时配置烧入。**worker** `OVT_WATERMARK_FONT`（部署级 CJK 字体路径、非逐语；缺/非文件优雅回落 ffmpeg 默认字面、不崩任务）；Dockerfile 装 `fonts-noto-cjk`。**admin UI** 新增「AIGC·视频水印」组（开关+文案+位置+大小+透明度+取色器；新增 color 字段类型）。 | feat/aigc-video-watermark（feat `72c28a9` + 审修 `ad48068`，squash `9d1effc`） | #79（squash `9d1effc`） | **对抗多透镜 workflow（6 镜 + 逐项验真）→ 1 P2**（§3 审计 under-claim：subtitle_only+burned 字幕关水印开记 None）**已修 + 复验**（over-claim 已证不可能：applied+None-embed_method 只能来自水印路径、必有真视频产物）；**CodeX CLI + @codex bot 本轮均遭 OpenAI 用量封顶不可用**（~7/9 复位；业主拍板凭 CI + 对抗审基线合并） | 本地全绿（autodub-core **pytest 244**〔drawtext 构建器/burn+第二趟集成/缓存失效/visible_watermark manifest〕 + schemas 11 + worker 119 + provider-adapters 96 + local-runner 34；control-plane **vitest 281** + tsc；ruff/pyright(0)；codegen 无漂移；**headless 管理台**水印组渲染+取色器）+ **CI 5/5 绿** | ✅ **已合并 `9d1effc`（#79）+ 部署上线 + 真机验证**。部署顺序（关键）：**先重建 VPS worker**（薄补丁镜像 `FROM ovt/media-worker:latest` + `apt fonts-noto-cjk` + `ENV OVT_WATERMARK_FONT` + COPY 5 文件〔+ffmpeg_utils/pipeline〕——不重烤 2.4G 模型；旧 worker `extra=forbid` 会拒带新字段 job 故必先行；旧镜像标 `pre-pr2` 可回滚），后 deploy workflow（build SPA→wrangler deploy）控制面。真机 `/admin`：视频水印组默认**关** + 文案 `本视频由 AI 合成` + 位置 bottom_right + 字号 5% + 透明度 85 + 取色器 #ffffff + 6 项皆可编辑 + 无 console error。**默认关⇒对现有任务零行为变化，须业主在管理台显式开启（审计）方生效。** |
| 2026-07-05 | **前端反映运行时上限 + 上传进度（post-launch UX 修复，业主直接反馈）**：业主发现管理台改了 `maxVideoDurationMs` 但前端仍显示硬编码默认——前端 `caps.ts` 是编译期常量、无途径读运行时配置；服务端 ffprobe 复核才是权威门，故前端预警会误导（管理台调高→吓退用户；调低→前端放行但服务端拒）。**#1 修复**：新增**公开** `GET /api/config`（`none` 鉴权，`publicConfig`）返回**精选非敏感**投影——仅 `maxVideoDurationMs`（各档）+ `maxUploadBytes`，绝不泄运维敏感项（每日额度/TTL/queueBackend/token；测试断言无泄漏）；读 `ctx.config`（KV 快照）不打 D1。前端 `caps.ts` `fetchLimits` 加载拉活值、警告函数吃可选 `limits`（默认 `DEFAULT_LIMITS`）；**健壮**：网络错/非2xx/畸形/负值/缺字段逐字段回退、绝不抛、绝不返回会硬阻合法上传的值。**#2 上传进度**：`putSource` 改注入式 XHR uploader（fetch 无法报上传进度）+ `onProgress`；App.svelte onMount 拉 limits（与身份铸造并行、失败留默认）+ 细进度条 + 「上传中 NN%」。**#3 分段上传：业主决定暂缓**（R2 multipart 大投入换边际收益，500MiB/5min 尺度单次 PUT 够用；进度显示已覆盖体感）。 | feat/frontend-live-limits-upload-progress（squash `c1eddb3`, #80） | #80（squash `c1eddb3`） | **对抗多透镜 workflow（3 镜：公开端点信息泄漏 / fetchLimits 健壮性+不阻合法上传 / 上传进度+状态生命周期 + 逐项验真）→ 0 findings**；CodeX 两审级仍 OpenAI 用量封顶（业主授权凭 CI + 对抗审合并） | control-plane **vitest 282**（新增 /api/config 精选+无泄漏用例）+ tsc；web **svelte-check 0 + vitest 42**（fetchLimits 解析/回退/活值警告 · putSource uploader 委派+进度转发 · App 冒烟更新）+ SPA build；CI 5/5 | ✅ **已合并 `c1eddb3`（#80）+ 部署上线 + 真机验证**：仅控制面部署（无 schema/worker 改动、无需重建 worker）。真机 `GET /api/config` = 仅 `{maxVideoDurationMs,maxUploadBytes}`、无敏感键；**活值 = 三档皆 30 分钟**（业主此前把 dub/both 由默认 5min 调到 30min——正是本 bug 根因），SPA 加载即拉取该活值（headless 网络捕获 200）、masthead 渲染无 console error ⇒ 前端不再显示陈旧 5 分钟。 |
| 2026-07-05 | **前端模式提示也随活配置（#80 收尾 · 业主「核对一下」触发）**：核对发现 #80 只把动态警告改活,输出模式选项的**静态提示**仍硬编码 5/5/30 分钟(modes.ts)——真机 dub/both 活值已 30min,故「配音」提示仍显"5 分钟"、与旁边正确的"30 分钟"警告矛盾(同一陈旧上限 bug 第二处)。modes.ts 提示去掉分钟;App.svelte `modeCapMin` 从活 limits 派生、模板追加「，最长约 N 分钟」(响应 /api/config)。 | fix/mode-hint-live-limits（squash `00df13f`, #81） | #81（squash `00df13f`） | 平凡机械派生(无新面、模式同 #80 已审)→ 免独立 workflow;CodeX 仍用量封顶 | web svelte-check 0 + vitest 43(新增 App 测试断言 dub 提示随活 30min)；CI 5/5 | ✅ **已合并 `00df13f`（#81）+ 仅控制面部署 + 真机验证**：三档模式提示皆「最长约 30 分钟」+ 警告亦 30min ⇒ UI 两处上限完全一致、随管理台活配置。 |

## 续跑点（resumption）
- **⏭ 已排期 · 新会话建（业主 2026-07-05 拍板，借鉴 pyvideotrans 扩为多引擎）：Tier 1 多引擎「手动选配音音色」= Piper + Kokoro-82M + edge_tts。** 起因：业主反馈配音全程单一女声——① 免费 ASR 不做说话人分离（全 `SPEAKER_00`，设计如此、多说话人/克隆归 Tier2/3 护城河，暂缓），② TTS 阶梯回落到唯一烤进镜像的 piper `huayan`（女；edge-tts 未装 + egress 挡微软端点）。**研究 workflow 结论（借鉴 pyvideotrans 但精选，非其 30 个——多为付费/GPU 克隆=Tier2/3 moat）：3 引擎按信任分层**——**Piper**（离线脊梁、最稳、中文仅 huayan 女）+ **Kokoro-82M**（离线、**唯一离线中文男声** 4F/4M、~170MB+torch/`misaki[zh]`、中文质量自评偏低但够免费层）+ **edge_tts**（音质最好/中文男声多/零 CPU，但微软非官方端点 403/失效→**best-effort+离线兜底**）。**跳过**：MeloTTS-local（冗余重）、CF MeloTTS（需 key+中文坏）、gTTS（单机械声）、所有克隆模型（GPU+moat）。**核心洞见：edge-only 与多引擎是同一设计的两片、共用同套管道——别做一次性 edge-only。** **片A**=provider-generic 能力清单 + 软-pin 路由 + edge 选择器；**片B**=烤 Piper+Kokoro 进镜像并注册为 free provider（纯打包、零设计改动）。**关键管道：** (1) **能力清单**：worker 发布本箱实装 引擎+音色（`probe()`+`voices_for`）→ 新 `POST /internal/providers/capabilities`（worker 鉴权、仅名）→ control-plane 并 circuit-breaker 快照 → 公开 `GET /api/tts/voices?target_lang=` → 前端据此渲染（**绝不静态列表**，每部署可用集不同）；(2) **软-pin 路由**：今 worker `_route_plan`（pipeline.py:436-447）无条件覆写 `plan.tts`→改为尊重 pin（`select()` base.py:256-266 本已认 pin）。**按失败类型分级**：pin 引擎本箱结构性缺失（不在 probe / 不覆盖 locale / piper 模型不符）→当场明确失败（`tts_provider_unavailable`/`no_tts_model_for_language`）；pin 引擎运行时瞬态熔断/429（常是他人配额）→不硬失败、回落自动路由 + 盖 `voice_substituted` 标记（段已存 tts_provider 可审计）+ 可选 `strict_voice`。**逐层改：** schema `JobPlan` 加 `tts_voice: str|null`（+可选 `voice_substituted`/`ErrorCode.tts_provider_unavailable`；`tts` 本已 str|null 可 pin provider）；control-plane createJob 收 tts_provider/tts_voice 经 `validateProvider` 校验（付费 403/未知 400）+ 上述两端点 + D1 `provider_capabilities` 表；kernel `_assign_voices`（stages.py:216）认 pin voice 覆盖 `voices[i%len]`、未 pin 走 voices_for；adapters 让 voices_for 回富元数据（id+人类标签）、edge voices_for 补 catalog（修无目录回落 en-US 坑）；worker 停覆写 pin + 能力发布 + 片B 烤 Piper/Kokoro；前端 引擎→音色 两级选择器 + 送 pin + 显示 substitution 提示。**开源核边界：** preset 选择器=Tier1（不泄 moat）；**硬护栏**：仅闭集 free preset 音色，**禁**参考样本上传/克隆/任意 voice 字符串（那是 Tier2/3）。**红线 §1 不动**：选项=free ladder、付费不可选。**风险：** ①舰队异构（pin 落到缺该引擎的箱）→按部署同构供给+别全局静态列表；②熔断 vs pin→软-pin+substitution；③Piper 每箱单模型（按实装 voices_for 反映）；④Kokoro 中文质量（设预期）；⑤moat 蔓延守闭集；⑥edge 脆（best-effort+离线兜底）。**待评估**：pyvideotrans 的 **VITS-cnen**（~175 中文声/CPU/免费）可能比 Kokoro 更适合离线中文多音色——片B 前核实真实可用性/体量/质量再敲定离线引擎组合。**diarization 暂缓**（业主"不能就算了"；sherpa-onnx CPU 可行但属 Tier2/3+加阶段/模型）。**CodeX 约 7/9 前用量封顶**→对抗 workflow + CI 兜。走 ship-unit（worker 先行；片B 完整重建镜像）。
- **已完成（全部并 main、state 同步）：** STEP0-A（#1，`214bd09`）+ STEP0-B（#2，`8f2c613`）+ STEP0-C（#3，`29e64b6`）+ **T1.1**（#4，`1adb87b`，PR #34）+ **T1.2**（#5，`9c42c4c`，PR #35）+ **T1.3 Batch A**（a/c/d/b，#6–#9，PR #36 squash `d093ec9`）+ **T1.3 Batch B**（e/f/g，#10–#12，PR #37 squash `7967e7a`）。i18n 闸 2026-06-23 开；#1–#12 = status:done、EPIC #30 #1–#12 已勾。
- **🎯 M1（本地管线打通）已完成（2026-06-24）、待项目主验收。** 已并：STEP0-A/B/C + T1.1 + T1.2 + T1.3a–g + **T1.4**（#38 squash `8891ea2`）。T1.4 并入点 `8891ea2`（main HEAD 含后续 docs/收尾 commit，以 `git log` 为准）；#1–#13 全 closed + `status:done`、EPIC #30 M1 段全勾。一条命令本地端到端可跑：`ovt run <file|url> -t <locale>` / `ovt doctor`，真出带标识 mp4+srt。
- **M1 收敛后归延后单元的项（备查，到对应单元再做）：** ① 显式 `--out` 复用陈旧缓存 → 内核按产物存在性缓存、不键控输入（T1.1 resumability 限制，深层 keying 属 M2-CLOSE）；薄 CLI 唯一默认目录已挡常见误用。② piper 单一 `FVD_PIPER_MODEL` 按目标语 vet → 需 per-language voice catalog（`assets/voices.json`，tts.py 注明 T1.3b/f 后续）。③ CF 源语完整检测 pass（当前无 hint 时 CF MT clean-fail）。
- **Batch B 收敛后 4 项运行期接线（T1.4 处理状态）：** ① 语言 gate 接准入＝`local_runner/admission.py`（done）· ② commercial-safe 运行期 TTS 选择＝`admission.py`（done）· ③ 供应链 pin 接 preflight＝`local_runner/doctor.py`（done）· ④ CF 源语解析：CF 不回检测语言→admission 用 `resolve_source_language(hint)`，无 hint 时当前 CF MT 步 clean-fail（明确报错，非静默）；完整检测 pass 属后续。
- **T1.1 resumability 边界（备查）：** 已把文件契约硬化到「每个 `.exists()` 缓存产物经 `atomic_output` 原子写 + 元数据持久化早于 skip-gate」。**更深的 worker 崩溃恢复 / 并发认领 / 租约语义属 M2-CLOSE §12 DoD**（lost-worker/幂等/TTL），非 T1.1 范围；worker 集成时复核。
- **T1.1 关键设计（备查）：** ① 契约复用 `ovt_schemas`（STEP0-B 真源），不再移植 dataclass；`from_dict/to_dict` → `model_validate/model_dump`。② provider seam = 依赖注入（core 定义结构化 `Protocol` + `Resolver`，不 import `provider-adapters`，保 T1.1 ∥ T1.2）；`select()`/ladder/`PAID_PROVIDERS` 仍归 T1.2。③ `assign_timing` 抽为纯函数（golden 守、免 ffmpeg）。④ `ingest` 仅本地文件，URL/yt-dlp 推迟 T1.4（backlog「URL/yt-dlp 仅此开」+ SSRF T1.3c），核保持无网。⑤ 已按 [[sanitization-needs-semantic-pass]] 语义脱敏（去 `[fvd]`/上游商业线引用）。
- **M2 进度（2026-06-25 起）：** **T2.0（GitHub issue #14，PR #40 squash `ec09b6e`）已完成**——M2 硬门槛两半皆绿：本地 342 测 + 真实 D1 remote PASS（run 28142927440）。issue #14 = closed + status:done、EPIC #30 #14 已勾。**T2.0 硬门过 → T2.1（issue #15）解锁**。**T2.1（issue #15，PR #41 squash `e12bfcb`）已完成**——control-plane Worker 闭环端点（/api 公开 + /internal）+ 完整 D1 schema + queue_adapter=D1-claim（复用 T2.0 乐观锁）；**真测试门** vitest+better-sqlite3（D1 即 SQLite，真 CLAIM_SQL 跑真引擎）**40 测**；外审 对抗 workflow + CodeX CLI(4) + @codex bot R1(3)/R2(2)/R3(1) **descending 收敛**。R3 P1（上传 HEAD 后 swap TOCTOU）→ 上传 PUT TTL 收窄 15min + **路由 T2.4**（worker ffprobe 复核为权威门，设计 §pipeline 行 52；项目主拍板「合并+路由 T2.4」）。issue #15 = closed + status:done、EPIC #30 #15 已勾。**T2.2（issue #16，PR #42 squash `46c2201`）已完成**——Python 桩 worker（plan §6）：闭环 claim→拷输出→complete + 30s 独立心跳续租（硬封顶停续租 + 精确 `processing_timeout`）+ try/finally 清盘 + 启动清孤儿 + workdir cv 隔离；零三方依赖 stdlib SigV4（AWS KAT 钉死）；**真测试门** pytest **32 测**；外审 对抗 workflow + CodeX CLI **5 轮**（功能→韧性→超时精确→scratch 隔离，descending 收敛、超时主题闭）+ @codex bot 终审 clean；CI 4 job 绿。**归 DEPLOY/M2-CLOSE：多 worker 进程共享 OVT_WORKDIR 的清孤儿 ownership/lock 硬化**（部署=单容器/箱独占卷、全清正确）。issue #16 = closed + status:done、EPIC #30 #16 已勾。**T2.3（issue #17，PR #43 squash `342b923`）已完成**——sweeper（CF Cron `scheduled`，每分钟）四职责：TTL 前缀清产物+源(AD-17) · 租约过期重排/`worker_lost`(H1) · 上传孤儿 claim-then-delete · queue reconciler(D1 权威 worklist)；**claim_version 守**（重排后 complete/fail/心跳均 409，修 jobs.ts 假成功+cv1 产物泄漏）；崩溃幂等（孤儿用内部列 `source_purged_at` 重试标记，迁移走前向 0002 不改 0001）+ 逐行/职责隔离 + 批量界。**真测试门** vitest **64 测**（sweep 24）+ tsc 0；外审 对抗 workflow(3 修) + CodeX CLI **5 轮 descending**（生命周期/竞态→可重试→崩溃幂等/陈旧产物/批量界→契约枚举→前向迁移；§7 停 CLI）+ @codex bot 终审 clean；CI 4 job 绿。**归 M2-CLOSE：`deadline_at` 跨模式档反饿死**；**归 T2.4/M2-CLOSE：`worker_lost` 配额退还**。issue #17 = closed + status:done、EPIC #30 #17 已勾。**T2.4（issue #18，PR #44 squash `354ead4`）已完成**——abuse gate 骨架 + worker ffprobe 复核 + SSRF/presign 守卫 CI + nftables egress 雏形：worker claim 后对真实字节重核（HEAD 大小预检 + 下载界读闭合 swap 竞态 + 格式 allowlist + per-mode 时长 cap〔`both` 独立〕→ fail+删源、先报后删保重排幂等），**闭合 T2.1 上传 swap TOCTOU 端到端**；Turnstile(配 `TURNSTILE_SECRET_KEY` fail-closed)+per-IP /64 key（双池计数留 M2-CLOSE）；SSRF/presign 守卫 CI 新 job（按函数体作用域）；egress 雏形默认 drop+封 IMDS/RFC1918/IPv6+作用域化重置。**真测试门** vitest 71〔abuse 7〕+ pytest 397〔admission 8/worker 22/guardrail 10〕+ ruff/pyright 0/redline 25/schema no-drift；外审 对抗 workflow(6 修) + CodeX CLI **5 轮 descending**（OOM→竞态→Turnstile 绑定名→守卫作用域→nftables flush/accept；运行时自 R3 收敛）+ @codex bot 终审 1 advisory P2（per-mode `both` cap）已修；CI 5 job 绿。**归 M2-CLOSE：双池 cap 原子计数**；**归 DEPLOY：egress DNS 刷新活规则**。issue #18 = closed + status:done、EPIC #30 #18 待勾。
  - ⚠️ 编号对位（防混淆）：M2 单元的 GitHub issue 号 = T2.x 偏移 14——**T2.0=#14 · T2.1=#15 · T2.2=#16 · T2.3=#17 · T2.4=#18 · T2.5=#19 · T2.6=#20** · SECRETS=#21 · CFG-GUARD=#22 · FREE-POOL=#23 · OBS=#24 · DEVLOOP=#25 · M2-CLOSE=#26。（勿把内部 todo 号当 issue 号。）
- **下一步：** **M1 已验收**（项目主 2026-06-25）。**M2 已验收 + 授权进入 M2.1**（项目主 + CodeX M2 审计 2026-06-30，~86/100；节奏 = 自主推进〔同 M2〕，停在 M2.1→M3 边界）。M2 内所有单元（T2.0–T2.6 + SECRETS + CFG-GUARD + FREE-POOL + OBS + DEVLOOP〔PR #51 `6d82a99`〕）+ **M2-CLOSE(#26) 全 4 PR** 均已并 main、state 同步：✅ PR-A（真管线 + FREE-POOL `route_free` 路由 + 凭据→adapter 活注入 + provider/chunk/free_pool_result 遥测真值，#52 `4f39c5d`）· ✅ PR-B（双池 daily cap〔global/actor/ip 原子计数 + 全局 cap + 权威 counters 表〕 + worker_lost 退还 + anon_id 服务端 HMAC + cap 逼近告警，#53 `9559bfc`）· ✅ PR-C（deadline-aware comparator + `deadline_exceeded` + `enforceDeadlines` 硬-SLA + 错误码注册表收口 + §12 DoD 门；`lease_renewed_at` 已弃，#54 `eff9005`）· ✅ PR-D（worker 并发 ≤ `worker_concurrency`〔默认 1，§14 2GB 基准〕 + `light_slot_reserve` 轻槽预留 `free_min_share`，#55 `272cc17`〔6-lens 自审 1 P3 默认值对齐 + CodeX CLI clean + @codex bot clean + CI 5/5〕）。 **⇒ 原「归 M2-CLOSE」延后项已由 A/B/C/D 落地**：route_free 真管线接线 + 凭据活注入 + 遥测真值、双池原子计数 + 全局 cap + 权威 counters 表、worker_lost 配额退还、anon_id 服务端 HMAC、deadline_at 跨模式反饿死 comparator + 错误码、worker 并发 + 轻槽预留；`lease_renewed_at` 已弃（heartbeat 用 `lease_expires_at` 编码末次续租）。 **✅ M2.1 烧字幕端到端已并 main**（#58 squash `3dffacc`，2026-07-01；对抗审 + CodeX CLI + @codex bot 4 遍 = 11 审改全修、0 P1；pytest 512 / CP vitest 263 / web 30 / CI 5/5）。**#27 仍 OPEN**：剩 **目标语广集**（逐语 `language_capabilities` vet 扩充，单独 PR 收口 #27）。**⏸ 现停在 M2.1→M3 边界；M3 及 #27 收口节奏待项目主指示。公网 go-live 单独 gate、未授权。** **仍延后（非 M2 代码缺口，re-home）：** ① **SPA 调 `POST /api/anon` 取签名 anon id 接线 = P1 GO-LIVE BLOCKER**（apps/web；接线前不注入 `ANON_ID_HMAC_KEY`、不开公网生产流量）· ② worker 按 job `settings_version` admit〔精确分钟 pinning〕 + 按 ffprobe 实时长扣分钟对账〔worker-accounting〕 → M2.1/DEPLOY · ③ CFG-GUARD 并发多 admin 写串行化〔Durable Object 单写者〕 + worker 按 `settings_version` 拉 `/internal/config?version` → M2.1/DEPLOY · ④ T2.5 worker 订阅 CF Queues〔真降延〕 + 节流 stale 重发〔需 `last_wake_at` 列〕 → DEPLOY · ⑤ T2.2 多进程共卷清孤儿 ownership/lock 硬化 → DEPLOY · ⑥ T2.6 跨域 CORS + Turnstile token live-gate UX → DEPLOY · ⑦ T2.4 egress nftables 活规则 DNS 刷新 → DEPLOY · ⑧ CFG-GUARD T2.5 break-glass 生产锁 `queue_backend` 切换审计 → CFG-GUARD/DEPLOY · ⑨ AIGC 审计化开关 → **业主授权已落地 + 上线**：PR-1（#78 字幕通道）+ PR-2（#79 视频可视水印）两通道均已并 main（`ce98b02` / `9d1effc`）+ 部署（worker 先行薄补丁 + deploy workflow）+ 真机验证；管理台 `/admin` 可调、每改经 CFG-GUARD 审计、默认合规姿态（字幕默认开 / 视频水印默认关且不去容器法定标识）。**唯一仍不暴露**：master `enabled` 全局禁用开关 → 仍归项目主（红线 3 最强动作，不自主引入）。 **⚠️ 上线 go-live 非我能独力完成**：需项目主 CF 账号 + provider key（不经我）+ VPS SSH（Hetzner 4GB prod / Volcano 2GB dev，x86）+ 律师/域名 + **真实 CF/VPS + 测试视频端到端冒烟** + go-live sign-off；代码至多到 deploy-ready + runbook。**M2.1 / M3 里程碑边界仍待项目主逐道 sign-off。**
- **DAG 提示：** STEP0-B/C → T1.1 ∥ T1.2 → T1.3a–g（∥）→ T1.4（= M1）→ **T2.0（M2 硬门，先于 T2.1）** → T2.1 …。
- **schemas 真源摘要：** 17 实体（Job/Transcript/Word/TranscriptLine/DubbingSegment/TranslationResult/Cue/UploadSession/AigcMarking/JobPlan/JobArtifacts/ModelRef/WorkerMeta/Manifest/LanguageCapability/LanguageCapabilities + ErrorCode 枚举）。待复审解读点：`settings_version`=int、`counted_*`=幂等 bool、`Cue`（双语 source/target）、`worker_meta`（ffprobe blob + 模型 sha）、`Manifest.job`=完整 Job（非子集投影）。
