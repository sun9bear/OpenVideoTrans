[project]
name = "autodub-core"
version = "0.0.1"
description = "Language-agnostic pipeline core for OpenVideoTrans (Tier 1 MVP skeleton)"
requires-python = ">=3.11"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/autodub_core"]
