[project]
name = "provider-adapters"
version = "0.0.1"
description = "Provider adapter stubs for OpenVideoTrans (Tier 1 MVP skeleton)"
requires-python = ">=3.11"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/provider_adapters"]
