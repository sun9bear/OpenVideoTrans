# STEP0-A skeleton — placeholder test so pytest collects at least one item.
import provider_adapters


def test_package_importable() -> None:
    """provider-adapters is importable (skeleton)."""
    assert provider_adapters is not None
